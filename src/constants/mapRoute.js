/** Pedestrian route line on dark map (matches NavigationScreen). */
export const MAP_ROUTE_STROKE = '#c9a020';
export const MAP_ROUTE_OUTLINE = 'rgba(201,160,32,0.38)';

/** Skip routing when GPS and target are implausibly far apart (emulator abroad). */
export const MAX_ROUTE_CROW_METERS = 380_000;
